🛡️ TON Tunnel — Техническое описание (исправленное)

Проект: TON Tunnel
Автор: lucky
Протокол: HOPE (High-performance Overlay Protocol for Egress) v0.1
Платформы: Android / Windows / Linux (VPS)

1. Концепция

TON Tunnel — это VPN-подобный туннель, в котором пользовательский трафик передаётся на выделенный сервер (VPS) через зашифрованный UDP-канал на базе ADNL (сети The Open Network).

⚠️ Важно:

Трафик НЕ маршрутизируется через сеть TON
Ноды TON не участвуют в передаче пользовательского трафика
Используется только ADNL как транспорт (point-to-point)

👉 По сути:
это обычный VPN с кастомным транспортом, а не «VPN внутри TON»

2. Что здесь реально происходит

Архитектура:

Клиент (Android / Windows)
        ↓
TUN / Wintun (захват трафика)
        ↓
tun2socks / lwIP (TCP стек)
        ↓
SOCKS5
        ↓
HOPE (кастомный протокол)
        ↓
ADNL (UDP, шифрование)
        ↓
VPS (egress сервер)
        ↓
Интернет
3. Роль TON / ADNL

ADNL используется как:

🔐 Зашифрованный UDP-транспорт
🔑 Идентификация через Ed25519 ключи
📡 Возможный discovery (через DHT)

Но:

❌ Нет маршрутизации через ноды
❌ Нет multi-hop (как в Tor)
❌ Нет анонимной сети

👉 Это прямое соединение клиент → сервер

4. Протокол HOPE

HOPE — это минималистичный стрим-протокол поверх ADNL, который:

открывает TCP-потоки (OPEN)
передаёт данные (DATA)
закрывает соединения (CLOSE)
мультиплексирует потоки (stream_id)

👉 Он заменяет классические VPN-протоколы, но не делает сеть распределённой

5. Чем это является на самом деле

✔ Это:

кастомный VPN-туннель
TCP-over-UDP решение
obfuscation-транспорт (анти-DPI потенциал)
система с собственным протоколом (HOPE)

❌ Это НЕ:

Tor-аналог
децентрализованный VPN
маршрутизация через TON
«анонимная сеть»
6. Ключевая идея проекта

Главная ценность:

👉 маскировка трафика под ADNL / TON-подобный UDP поток

А не:

👉 «использование сети TON как транспортной инфраструктуры»

7. Итог

TON Tunnel — это:

VPN-туннель с кастомным протоколом (HOPE), который использует ADNL как зашифрованный UDP-транспорт для прямого соединения с VPS, без маршрутизации трафика через сеть TON.

8. Статус
Proof-of-concept: ✅
Передача трафика: ✅
Мультиплексирование: ✅
Оптимизация: в процессе
